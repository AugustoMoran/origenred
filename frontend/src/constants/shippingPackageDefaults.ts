/** Valores sugeridos al cargar producto (~ mochila). EnvíoPack usa estos si no guardás otros. */
export const DEFAULT_PRODUCT_WEIGHT_KG = 1;
export const DEFAULT_PRODUCT_DIMENSIONS_CM = {
  length: 15,
  width: 30,
  height: 45,
};
